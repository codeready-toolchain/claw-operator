apiVersion: v1
kind: Service
metadata:
  name: CLAW_INSTANCE_NAME
  labels:
    app: claw
spec:
  type: ClusterIP
  selector:
    app: claw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
