apiVersion: v1
kind: Service
metadata:
  name: claw-proxy
  labels:
    app: claw-proxy
spec:
  type: ClusterIP
  selector:
    app: claw-proxy
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
