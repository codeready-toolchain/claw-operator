apiVersion: v1
kind: Service
metadata:
  name: CLAW_INSTANCE_NAME-device-pairing
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: claw-device-pairing
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
