apiVersion: v1
kind: Service
metadata:
  name: openclaw-proxy
  labels:
    app: openclaw-proxy
spec:
  type: ClusterIP
  selector:
    app: openclaw-proxy
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
