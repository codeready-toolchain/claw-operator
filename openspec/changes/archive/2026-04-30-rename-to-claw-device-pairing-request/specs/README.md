# Specifications

This change is a pure renaming refactor with no behavioral modifications. All existing functionality of the DevicePairingRequest CRD is preserved under the new name ClawDevicePairingRequest.

**No specification changes required** - all existing requirements remain unchanged. The only change is the naming of types, files, and generated CRD manifests.
